# ─────────────────────────────────────────────
# RAG-LLaMA3 · Production Requirements
# Python 3.10+
# ─────────────────────────────────────────────

# ── Core LLM & Orchestration ──────────────────
langchain==0.2.11
langchain-community==0.2.10
langchain-core==0.2.23
llama-cpp-python==0.2.90          # INT4 quantised inference via llama.cpp

# ── Vector Store ─────────────────────────────
faiss-cpu==1.8.0                  # CPU build; swap to faiss-gpu on GPU nodes
sentence-transformers==3.0.1      # Embedding model (all-MiniLM-L6-v2 default)

# ── AWS SDK ──────────────────────────────────
boto3==1.34.144
botocore==1.34.144

# ── API Server ───────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.3
sse-starlette==2.1.3              # Server-Sent Events for streaming
pydantic==2.8.2
python-multipart==0.0.9

# ── RLHF Feedback Loop ───────────────────────
sqlalchemy==2.0.31
alembic==1.13.2
psycopg2-binary==2.9.9            # Postgres driver for feedback DB

# ── Observability ────────────────────────────
prometheus-client==0.20.0
opentelemetry-sdk==1.25.0
opentelemetry-exporter-otlp==1.25.0
structlog==24.2.0

# ── Document Loaders ─────────────────────────
pypdf==4.3.1
python-docx==1.1.2
unstructured==0.15.0              # Multi-format doc parsing

# ── Utilities ────────────────────────────────
python-dotenv==1.0.1
httpx==0.27.0
tenacity==8.5.0                   # Retry logic
tqdm==4.66.4
numpy==1.26.4
pandas==2.2.2

# ── Dev / Test ───────────────────────────────
pytest==8.3.1
pytest-asyncio==0.23.8
pytest-cov==5.0.0
httpx==0.27.0                     # async test client
ruff==0.5.4                       # linter
black==24.4.2                     # formatter
